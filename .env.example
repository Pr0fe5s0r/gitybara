# gitybara env (copy to .env and fill in)

# GitHub PAT — set during `gitybara init`, stored in ~/.gitybara/config.json
# GITHUB_TOKEN=

# Override log level (trace | debug | info | warn | error)
# LOG_LEVEL=info

# Port override (also configurable via `gitybara start --port`)
# GITYBARA_PORT=4242
